{
  "name": "webhook-delivery-service",
  "version": "1.0.0",
  "description": "Reliable webhook delivery service with retries, signing, and dashboard",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "express": "^4.18.3",
    "micromatch": "^4.0.5",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=20"
  }
}
